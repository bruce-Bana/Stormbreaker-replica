# Stormbreaker-replica
A covert surveillance system: a hidden client collects data from devices and sends it through anonymous networks to a central server. Administrators use a dashboard to view data, control devices remotely, and watch screens live.
